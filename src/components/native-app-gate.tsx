'use client'

import { WelcomeScreen } from '@/components/welcome-screen'

/**
 * App entry: welcome screen with name input and direct playback.
 */
export const NativeAppGate = () => {
  return <WelcomeScreen />
}
